<h1>User info</h1>

<%= form_for(@user) do |f| %>
	<p>name</p>
	<%= f.text_field :name %>
	<p>Image</p>
	<%= f.attachment_field :profile_image %>
	<p>introduction</p>
	<%= f.text_area :introduction %>
	<%= f.submit 'Update User' %>
<% end %>